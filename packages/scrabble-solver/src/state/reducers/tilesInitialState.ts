// TODO: when config is changed, initialize this with empty array of appropriate length
const tilesInitialState: (string | null)[] = [null, null, null, null, null, null, null];

export default tilesInitialState;
