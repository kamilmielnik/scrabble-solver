import { literaki } from '@scrabble-solver/configs';

// TODO: use local storage to initialize the state based on last session
const configReducerInitialState = literaki;

export default configReducerInitialState;
